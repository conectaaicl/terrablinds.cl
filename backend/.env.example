# ============================================
# TerraBlinds Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ---------- Server ----------
PORT=5000
NODE_ENV=development
# NODE_ENV=production

# ---------- Database (PostgreSQL) ----------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=terrablinds_db
DB_USER=terrablinds
DB_PASSWORD=CHANGE_ME_secure_password

# ---------- Authentication ----------
# REQUIRED: Generate a secure random string (min 32 chars)
# Example: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32

# ---------- Admin User (for seed script) ----------
ADMIN_EMAIL=admin@terrablinds.cl
ADMIN_PASSWORD=CHANGE_ME_min_8_chars

# ---------- CORS ----------
# Comma-separated list of allowed frontend origins
# CORS_ORIGINS=https://terrablinds.cl,https://www.terrablinds.cl
CORS_ORIGINS=http://localhost:5173

# ---------- URLs (required for payment redirects) ----------
# Backend public URL (where Flow.cl sends callbacks)
BASE_URL=http://localhost:5000
# Frontend public URL (where user is redirected after payment)
FRONTEND_URL=http://localhost:5173

# ---------- Payment - Flow.cl ----------
# Get your keys at https://www.flow.cl
FLOW_API_KEY=
FLOW_SECRET_KEY=
# Sandbox: https://sandbox.flow.cl/api
# Production: https://www.flow.cl/api
FLOW_API_URL=https://sandbox.flow.cl/api

# ---------- Email - Resend ----------
# Get your API key at https://resend.com
RESEND_API_KEY=
# Verified sender email in Resend
RESEND_FROM_EMAIL=TerraBlinds <noreply@terrablinds.cl>
